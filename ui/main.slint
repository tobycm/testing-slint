import { Button, VerticalBox } from "std-widgets.slint";
export component Main {
    VerticalBox {
        alignment: start;
        Text {
            text: "Hello!";
            font-size: 24px;
            horizontal-alignment: center;
        }
        HorizontalLayout {
 alignment: center; Button { enabled: false;
     text: "OK!"; } }
    }
}
